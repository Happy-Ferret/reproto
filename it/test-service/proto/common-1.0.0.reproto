type Entry {
  name: string;
}

/// Common error message returned.
type ErrorMessage {
  /// Message of the error.
  message: string as "message message";
  /// Status code returned with the error.
  status_code: unsigned;
}
